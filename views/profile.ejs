<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Dashboard</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
<div class="w-full min-h-screen bg-zinc-900 text-white p-4 md:p-10">
    <% if (success && success.length) { %>
        <div class="bg-green-700 text-white px-4 py-2 rounded mb-4"><%= success %></div>
    <% } %>
    <% if (error && error.length) { %>
        <div class="bg-red-700 text-white px-4 py-2 rounded mb-4"><%= error %></div>
    <% } %>
    <div class="flex justify-between items-center mb-8">
        <h3 class="text-3xl font-bold"><span class="font-light">Hello</span>, <%= user.name %></h3>
        <a class="bg-red-500 hover:bg-red-600 transition-colors rounded-md px-4 py-2 text-sm font-bold" href="/logout" aria-label="Logout">Logout</a>
    </div>
    <form action="/profile/privacy" method="POST" class="mb-6">
        <label for="privacy" class="text-sm font-semibold mr-2">Profile Privacy:</label>
        <select name="privacy" id="privacy" class="px-2 py-1 rounded-md bg-zinc-900 border border-zinc-700 text-white">
            <option value="public" <%= user.privacy === "public" ? "selected" : "" %>>Public</option>
            <option value="private" <%= user.privacy === "private" ? "selected" : "" %>>Private</option>
        </select>
        <button type="submit" class="ml-2 px-3 py-1 rounded bg-blue-500 text-white">Update</button>
    </form>
    <h5 class="mb-5 text-lg text-zinc-400">Create a new post</h5>
    <form action="/post" method="post" class="space-y-3 max-w-xl">
        <div>
            <label for="content" class="block mb-1 text-sm font-semibold">Your Post</label>
            <textarea id="content" name="content" required placeholder="What's on your mind?" class="p-3 w-full min-h-[80px] block resize-none outline-none bg-zinc-800 border-2 border-zinc-700 rounded-md focus:border-blue-500"></textarea>
        </div>
        <button type="submit" class="px-4 py-2 bg-blue-500 hover:bg-blue-600 transition-colors rounded-md text-white font-bold mt-2 w-full md:w-40">Create New Post</button>
    </form>
    <div class="posts mt-14 max-w-2xl">
        <h3 class="text-zinc-400 mb-2 text-lg">Your Posts</h3>
        <div class="space-y-5">
            <% user.posts.slice().reverse().forEach(function(post){ %>
                <div class="post bg-zinc-800 w-full p-4 rounded-md border border-zinc-700 shadow-sm">
                    <div class="flex items-center justify-between mb-2">
                        <h4 class="text-blue-500 font-medium">@<%= user.username %></h4>
                        <small class="text-zinc-400"><%= post.likes.length %> likes</small>
                    </div>
                    <p class="text-base tracking-tight mb-3"><%= post.content %></p>
                    <div class="btns flex gap-6">
                        <a class="text-blue-400 hover:underline font-semibold" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                        </a>
                        <a class="text-yellow-400 hover:underline font-semibold" href="/edit/<%= post._id %>">Edit</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</div>
</body>
</html>