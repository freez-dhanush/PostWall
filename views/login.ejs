<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Login</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
<% if (success.length) { %>
    <div class="bg-green-700 text-white px-4 py-2 rounded mb-4"><%= success %></div>
<% } %>
<% if (error.length) { %>
    <div class="bg-red-700 text-white px-4 py-2 rounded mb-4"><%= error %></div>
<% } %>
<div class="flex items-center justify-center min-h-screen bg-zinc-900 text-white">
    <div class="w-full max-w-md p-8 bg-zinc-800 rounded-lg shadow-lg">
        <h1 class="text-3xl font-bold mb-6 text-blue-400 text-center">Login to Your Account</h1>
        <form action="/login" method="post" class="space-y-4">
            <div>
                <label for="email" class="block mb-2 text-sm font-semibold">Email</label>
                <input id="email" name="email" type="email" required class="w-full px-3 py-2 rounded-md bg-zinc-900 border border-zinc-700 focus:border-blue-500 focus:outline-none" placeholder="Email Address" autocomplete="email"/>
            </div>
            <div>
                <label for="password" class="block mb-2 text-sm font-semibold">Password</label>
                <input id="password" name="password" type="password" required class="w-full px-3 py-2 rounded-md bg-zinc-900 border border-zinc-700 focus:border-blue-500 focus:outline-none" placeholder="Password" autocomplete="current-password"/>
            </div>
            <button type="submit" class="w-full py-2 mt-4 rounded-md bg-blue-500 text-white font-bold hover:bg-blue-600 transition-colors">Login</button>
            <p class="text-xs text-zinc-400 text-center mt-2">Don't have an account? <a href="/" class="text-blue-400 underline">Sign up</a></p>
        </form>
    </div>
</div>
</body>
</html>