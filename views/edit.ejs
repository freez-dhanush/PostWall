<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Edit Post</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
<% if (success.length) { %>
    <div class="bg-green-700 text-white px-4 py-2 rounded mb-4"><%= success %></div>
<% } %>
<% if (error.length) { %>
    <div class="bg-red-700 text-white px-4 py-2 rounded mb-4"><%= error %></div>
<% } %>
<div class="w-full min-h-screen bg-zinc-900 text-white p-4 md:p-10 flex items-center justify-center">
    <div class="w-full max-w-lg bg-zinc-800 p-8 rounded-lg shadow-lg">
        <div class="flex justify-end mb-4">
            <a class="bg-red-500 hover:bg-red-600 transition-colors rounded-md px-4 py-2 text-sm font-bold" href="/logout" aria-label="Logout">Logout</a>
        </div>
        <h2 class="mb-5 text-2xl text-blue-400 font-bold">Edit Your Post</h2>
        <form action="/update/<%= post._id %>" method="post" class="space-y-3">
            <div>
                <label for="content" class="block mb-1 text-sm font-semibold">Your Post</label>
                <textarea id="content" name="content" required class="p-3 w-full min-h-[80px] block resize-none outline-none bg-zinc-900 border-2 border-zinc-700 rounded-md focus:border-yellow-500"><%= post.content %></textarea>
            </div>
            <button type="submit" class="px-4 py-2 bg-yellow-500 hover:bg-yellow-600 transition-colors text-black font-bold rounded-md mt-2 w-full md:w-40">Update</button>
        </form>
    </div>
</div>
</body>
</html>